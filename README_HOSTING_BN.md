
# 🚀 GitHub ও হোস্টিং সেটআপ গাইড

আপনার প্রোজেক্টটি GitHub-এ কানেক্ট করতে এবং লাইভ করতে নিচের ধাপগুলো অনুসরণ করুন।

## ১. GitHub-এ কোড আপলোড করার ধাপসমূহ

প্রথমে [GitHub.com](https://github.com) এ গিয়ে একটি নতুন **Repository** তৈরি করুন (নাম দিন: `rajbari-dist-info`)। তারপর আপনার পিসিতে টার্মিনাল ওপেন করে নিচের কমান্ডগুলো দিন:

```bash
# ১. গিট ইনিশিয়ালাইজ করুন
git init

# ২. সব ফাইল যোগ করুন
git add .

# ৩. প্রথম কমিট করুন
git commit -m "Initial commit - Rajbari App"

# ৪. মেইন ব্রাঞ্চ তৈরি করুন
git branch -M main

# ৫. আপনার GitHub লিঙ্কের সাথে কানেক্ট করুন (আপনার লিঙ্কটি দিন)
git remote add origin https://github.com/আপনার-ইউজার-নেম/rajbari-dist-info.git

# ৬. কোড পুশ করুন
git push -u origin main
```

## ২. Vercel বা Netlify এর মাধ্যমে কানেক্ট (সহজ পদ্ধতি)
আপনার যদি নিজের ডোমেইন থাকে, তবে GitHub-এর সাথে **Vercel** কানেক্ট করা সবচেয়ে বুদ্ধিমানের কাজ। এর ফলে:
- আপনি GitHub-এ কোড পরিবর্তন করলে ওয়েবসাইট অটোমেটিক আপডেট হয়ে যাবে।
- আপনাকে বারবার ম্যানুয়ালি ফাইল আপলোড করতে হবে না।

**কিভাবে করবেন?**
১. [Vercel.com](https://vercel.com) এ যান।
২. "Add New Project" এ ক্লিক করুন।
৩. GitHub সিলেক্ট করে আপনার `rajbari-dist-info` রিপোজিটরি ইমপোর্ট করুন।
৪. "Deploy" বাটনে ক্লিক করুন।

## ৩. নিজের ডোমেইন সেটআপ
Vercel-এ সাইট লাইভ হয়ে গেলে:
১. Vercel Dashboard-এ গিয়ে **Settings > Domains**-এ যান।
২. আপনার কেনা ডোমেইনটি (যেমন: `www.yourdomain.com`) যোগ করুন।
৩. আপনার ডোমেইন প্রোভাইডারের (যেমন: Namecheap বা Bluehost) DNS সেটিংস-এ Vercel-এর দেওয়া IP বা CNAME বসিয়ে দিন।

---
**মনে রাখবেন:** আমি কোডে আপনার **Supabase URL** এবং **Key** ডিফল্ট হিসেবে দিয়ে দিয়েছি, তাই সরাসরি কানেক্ট হয়ে যাবে।
